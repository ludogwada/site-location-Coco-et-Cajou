import React, { useState } from 'react';
import Header from '../layout/Header';

const Home = () => {
	return (
		<>
			<Header page='home' />
			<main className='home'>
				<section></section>
			</main>
		</>
	);
};

export default Home;
