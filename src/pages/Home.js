import React, { useState } from 'react';
import Header from '../layout/Header';

const Home = () => {
	return (
		<>
			<Header page='home' />
			<main className='home'></main>
		</>
	);
};

export default Home;
